def runningSum(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        newlist = []
        f = 0
        for a in nums:
            f = f + a
            newlist.append(f)
        return newlist
