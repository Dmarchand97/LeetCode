 final_list = []
    for number in num:
        x = number
        final_list.append(x)
        y = num[n]
        final_list.append(y)
        n = n + 1
        if y == num[-1]:
            return final_list
